services:
  pml-web:
    build:
      context: .
      dockerfile: Dockerfile.web
    container_name: pml-web
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
